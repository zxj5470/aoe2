# 阶段 4/5/6 补全实施计划

> **For Claude:** Use `${SUPERPOWERS_SKILLS_ROOT}/skills/collaboration/executing-plans/SKILL.md` to implement this plan task-by-task.

**Goal:** 补全建筑建造系统、完善战斗系统、实现生产系统，使游戏具备"建造→生产→战斗"的核心游戏循环。

**Architecture:** 在现有架构上增量实现。建筑建造修复 BuildingPlacementSystem 的异步 bug 并接入建造动画流程；战斗系统补充单位死亡清理和基础 AI 状态机；生产系统让建筑队列真正产出单位并写入科技研究框架。

**Tech Stack:** JavaScript ES6 Modules, Three.js (^0.160.0), RBush (^4.0.1), Vite

---

## 背景分析：当前代码的关键缺陷

| # | 缺陷位置 | 问题描述 | 状态 |
|---|---------|---------|------|
| 1 | `BuildingPlacementSystem.js:223` | `createBuilding()` 使用 `import()` 异步但 **没有 return**，导致 `placeBuilding()` 返回 `null` | ✅ 已修复（改为静态 import + 同步创建） |
| 2 | `Building.js` | `onProductionComplete()` 只调用 `onComplete` 回调，**不实际生成单位** | ⏳ 待修复 |
| 3 | `ActionPanel.js` | `handleProductionCommand` 调用 `addToProductionQueue` 但队列项**没有 onComplete** | ⏳ 待修复 |
| 4 | `Entity.js:91` | `die()` 只设置 `isAlive=false`，**无死亡动画、无场景移除** | ⏳ 待修复 |
| 5 | `CombatSystem.js` | 基础攻击/克制已完成，**无 AI 行为** | ⏳ 待修复 |
| 6 | `Building.js` | `isUnderConstruction` 字段存在但**放置时不设置为 true**（Bug #1 导致） | ✅ 已修复（Bug #1 修复后正常工作） |

---

## Task 0: 建筑放置右键取消 + 修复双触发 Bug ✅ 已完成

**修改内容:**
- `EventManager.js` — 移除 `onContextMenu` 中的重复 `handleRightClick` 调用
- `Game.js` — `handleRightClick()` 开头增加 `isPlacing` 检查，右键取消放置
- `EventManager.js` — `onKeyDown` 添加 Escape 键取消放置逻辑
- `ActionPanel.js` — 新增 `clearActiveBuildingButton()` 方法

---

## Task 1: 修复 BuildingPlacementSystem 异步 Bug ✅ 已修复

原 Bug 已不存在。`createBuilding()` 已使用静态 `import Building from '../entities/Building.js'`，同步创建并 `return building`。

---

## Task 2: 修复放置建筑时的建造流程 ✅ 已完成

当前代码已正确实现：
- `placeBuilding()` 返回有效建筑对象
- `Game.handleLeftClick()` 调用 `addEntity(building)` 加入游戏
- `EntityManager.addEntity()` 注入 `_game` 引用、添加场景、注册碰撞/空间索引/路径缓存

**Files:**
- Modify: `src/systems/BuildingPlacementSystem.js`

**Step 1: 定位 Bug**

`createBuilding()` (第 223 行) 内部用了 `import()` 动态导入，但是：
- 该函数没有 return
- `import()` 返回 Promise，但 `createBuilding` 不是 async
- `placeBuilding()` 调用 `this.createBuilding(config, worldX, worldZ)` 拿到的是 `undefined`

**Step 2: 改为同步导入**

将顶部的 Building 导入改为静态 import：

```javascript
import Building from '../entities/Building.js';
```

**Step 3: 重写 createBuilding()**

将异步动态导入替换为同步创建：

```javascript
createBuilding(config, x, z) {
    const building = new Building({
        name: config.name,
        buildingType: this.currentBuildingType,
        x: x,
        y: 0,
        z: z,
        width: config.width,
        depth: config.depth,
        height: config.height,
        health: config.health,
        maxHealth: config.health,
        owner: 'player',
        isUnderConstruction: true,
        constructionProgress: 0
    });

    building.createMesh();
    return building;
}
```

**Step 4: 验证 placeBuilding() 返回有效建筑对象**

在 `placeBuilding()` 末尾确认返回值非 null：

```javascript
const building = this.createBuilding(config, worldX, worldZ);
// building 现在是有效的 Building 实例
```

**Step 5: 运行 `npm run dev` 验证无报错，放置建筑能正常创建**

---

## Task 2: 修复放置建筑时的建造流程

**Files:**
- Modify: `src/systems/BuildingPlacementSystem.js`
- Modify: `src/core/Game.js`

**Step 1: 在 Game.js 中添加 addBuildingToScene 方法**

当 BuildingPlacementSystem 放置建筑后，需要将建筑添加到 Game 的实体列表和场景中。

在 `Game.js` 的 `initMapDependentSystems()` 或 `setupEventListeners()` 中，确保 `placeBuilding()` 的返回值通过 `this.addEntity(building)` 加入游戏：

```javascript
// 在 InputHandler 的鼠标点击事件中（已有的建筑放置逻辑处）
// 确认建筑放置后调用:
const building = this.buildingPlacementSystem.placeBuilding(
    mouseWorldPosition, this.player.resourceManager
);
if (building) {
    this.addEntity(building);
}
```

**Step 2: 检查 InputHandler 中建筑放置的鼠标事件处理**

搜索 InputHandler.js 中 `placeBuilding` 调用位置，确认流程是：
1. 鼠标左键点击
2. 如果 `buildingPlacementSystem.isPlacing === true`
3. 调用 `placeBuilding()` 获取建筑对象
4. 调用 `game.addEntity(building)` 将建筑加入游戏

**Step 3: 运行 `npm run dev`，放置建筑后确认建筑出现在场景中**

---

## Task 3: 实现建筑建造完成后的回调

**Files:**
- Modify: `src/entities/Building.js`

**Step 1: 完善 `onConstructionComplete()` 方法**

当前 `onConstructionComplete()` 只设置了 `this.health = this.maxHealth`。需要添加：
- 触发事件通知 Game/HUD
- 恢复建筑正常外观（缩放归 1）

```javascript
onConstructionComplete() {
    this.health = this.maxHealth;
    this.isUnderConstruction = false;
    
    if (this.mesh) {
        this.mesh.scale.y = 1;
    }
    if (this.symbolPlane) {
        this.symbolPlane.material.opacity = 1;
    }
    if (this.healthBarGroup) {
        this.healthBarGroup.visible = false;
    }
    
    // 如果是房屋，增加人口上限
    if (this.buildingType === 'house' && this.owner) {
        const game = this._game;
        if (game && game.player) {
            game.player.setMaxPopulation(game.player.population.max + 5);
        }
    }
}
```

**Step 2: 在 Building 构造函数或 Game.addEntity 中注入 `_game` 引用**

在 `Game.addEntity()` 方法中：

```javascript
addEntity(entity) {
    entity._game = this;
    this.entities.push(entity);
    // ... 现有的 mesh 添加到场景的逻辑
}
```

**Step 3: 运行 `npm run dev`，放置建筑后观察建造进度动画和完成事件**

---

## Task 4: 实现建筑队列单位生产

**Files:**
- Modify: `src/entities/Building.js` (`onProductionComplete`)
- Modify: `src/core/Game.js` (添加 `spawnUnitFromBuilding` 方法)
- Modify: `src/ui/ActionPanel.js` (确保 `addToProductionQueue` 传入正确的 `onComplete`)

**Step 1: 在 Building.js 中完善 onProductionComplete**

```javascript
onProductionComplete(productionItem) {
    if (productionItem.type === 'unit') {
        if (this._game) {
            this._game.spawnUnitFromBuilding(this, productionItem.unitType);
        }
    } else if (productionItem.type === 'research') {
        if (this._game) {
            this._game.applyResearch(this, productionItem.techType);
        }
    }
    
    if (productionItem.onComplete) {
        productionItem.onComplete();
    }
}
```

**Step 2: 在 Game.js 中添加 spawnUnitFromBuilding 方法**

```javascript
spawnUnitFromBuilding(building, unitType) {
    const spawnOffset = this.getBuildingSpawnOffset(building);
    const spawnX = building.position.x + spawnOffset.x;
    const spawnZ = building.position.z + spawnOffset.z;
    
    const unitConfig = this.getUnitConfig(unitType);
    
    const unit = new Unit({
        unitType: unitType,
        name: `${building.owner}_${unitType}_${Date.now()}`,
        x: spawnX,
        z: spawnZ,
        owner: building.owner,
        health: unitConfig.health,
        maxHealth: unitConfig.health,
        speed: unitConfig.speed,
        attackDamage: unitConfig.attackDamage,
        attackRange: unitConfig.attackRange,
        attackSpeed: unitConfig.attackSpeed,
        armor: unitConfig.armor,
        sightRange: unitConfig.sightRange,
        pathfindingSystem: this.pathfinding,
        formationSystem: this.formationSystem,
        game: this
    });
    
    const mesh = unit.createMesh();
    if (mesh) {
        this.addEntity(unit);
    }
    
    return unit;
}

getBuildingSpawnOffset(building) {
    const halfW = (building.gridSizeX || 2) / 2 + 1;
    return { x: halfW, z: 0 };
}

getUnitConfig(unitType) {
    const configs = {
        villager:  { health: 25, speed: 5, attackDamage: 3, attackRange: 1, attackSpeed: 1, armor: 0, sightRange: 4 },
        soldier:   { health: 40, speed: 4, attackDamage: 6, attackRange: 1, attackSpeed: 1, armor: 1, sightRange: 4 },
        knight:    { health: 60, speed: 6, attackDamage: 10, attackRange: 1, attackSpeed: 0.8, armor: 2, sightRange: 4 },
        archer:    { health: 30, speed: 4, attackDamage: 5, attackRange: 5, attackSpeed: 1.2, armor: 0, sightRange: 6 },
        scout:     { health: 35, speed: 8, attackDamage: 3, attackRange: 1, attackSpeed: 1.5, armor: 0, sightRange: 6 }
    };
    return configs[unitType] || configs.soldier;
}

applyResearch(building, techType) {
    // 阶段 6 实现：科技效果应用
    console.log(`[Game] 研究完成: ${techType}`);
}
```

**Step 3: 在 Game.js 的 update() 循环中确保建筑 update 被调用**

检查 `Game.update()` 方法，确认有遍历 entities 并调用 `entity.update(deltaTime)` 的逻辑。如果建筑的 `updateProduction` 没被调用，队列永远不会推进。

**Step 4: 运行 `npm run dev`，选中城镇中心，点击"村民"按钮，观察村民是否在建造进度完成后生成**

---

## Task 5: 实现单位死亡和清理

**Files:**
- Modify: `src/entities/Entity.js`
- Modify: `src/entities/Unit.js`
- Modify: `src/core/Game.js`

**Step 1: 在 Entity.js 中完善 die() 方法**

```javascript
die() {
    this.isAlive = false;
    this.isSelected = false;
    
    if (this.mesh) {
        this.mesh.userData.dead = true;
    }
}
```

**Step 2: 在 Unit.js 中覆盖 die() 添加死亡动画**

```javascript
die() {
    super.die();
    this.currentAction = 'dying';
    this.animationState = 'dying';
    this.deathTimer = 0;
    this.deathDuration = 1.0;
}

updateDeathAnimation(deltaTime) {
    if (this.animationState !== 'dying') return false;
    
    this.deathTimer += deltaTime;
    const progress = Math.min(this.deathTimer / this.deathDuration, 1);
    
    if (this.mesh) {
        this.mesh.scale.y = 1 - progress;
        this.mesh.position.y = -progress * 0.5;
        
        if (this.mesh.children) {
            this.mesh.children.forEach(child => {
                if (child.material && child.material.opacity !== undefined) {
                    child.material.transparent = true;
                    child.material.opacity = 1 - progress;
                }
            });
        }
    }
    
    return progress >= 1;
}
```

**Step 3: 在 Unit.js 的 update() 方法中加入死亡动画更新**

```javascript
update(deltaTime) {
    if (!this.isAlive) {
        const deathComplete = this.updateDeathAnimation(deltaTime);
        if (deathComplete) {
            this._markedForRemoval = true;
        }
        return;
    }
    // ... 现有 update 逻辑
}
```

**Step 4: 在 Game.js 的 update() 方法中添加实体清理逻辑**

```javascript
// 在 update 循环末尾
for (let i = this.entities.length - 1; i >= 0; i--) {
    const entity = this.entities[i];
    if (entity._markedForRemoval) {
        this.removeEntity(entity);
    }
}
```

确保 `removeEntity()` 方法存在并正确从场景中移除 mesh：

```javascript
removeEntity(entity) {
    const index = this.entities.indexOf(entity);
    if (index > -1) {
        this.entities.splice(index, 1);
    }
    if (entity.mesh && entity.mesh.parent) {
        entity.mesh.parent.remove(entity.mesh);
    }
    if (entity.type === 'unit' && this.player) {
        this.player.removeUnit(entity);
    }
    if (this.combatSystem) {
        this.combatSystem.unregisterCombatant(entity);
    }
    if (this.spatialIndex) {
        this.spatialIndex.remove(entity);
    }
}
```

**Step 5: 运行 `npm run dev`，让单位生命值降至 0，观察死亡动画和场景移除**

---

## Task 6: 实现基础战斗 AI

**Files:**
- Create: `src/systems/AISystem.js`
- Modify: `src/core/Game.js`
- Modify: `src/entities/Unit.js`

**Step 1: 创建 AISystem.js**

```javascript
class AISystem {
    constructor(game) {
        this.game = game;
        this.aiUnits = [];
        this.updateInterval = 0.5;
        this.timeSinceLastUpdate = 0;
    }
    
    registerUnit(unit) {
        if (!this.aiUnits.includes(unit)) {
            this.aiUnits.push(unit);
        }
    }
    
    unregisterUnit(unit) {
        const index = this.aiUnits.indexOf(unit);
        if (index > -1) {
            this.aiUnits.splice(index, 1);
        }
    }
    
    update(deltaTime) {
        this.timeSinceLastUpdate += deltaTime;
        if (this.timeSinceLastUpdate < this.updateInterval) return;
        this.timeSinceLastUpdate = 0;
        
        for (const unit of this.aiUnits) {
            if (!unit.isAlive) continue;
            this.updateUnitAI(unit);
        }
    }
    
    updateUnitAI(unit) {
        switch (unit.aiState) {
            case 'idle':
                this.checkForThreats(unit);
                break;
            case 'patrol':
                this.updatePatrol(unit);
                this.checkForThreats(unit);
                break;
            case 'chase':
                this.updateChase(unit);
                break;
            case 'attack':
                this.updateAttack(unit);
                break;
            case 'flee':
                this.updateFlee(unit);
                break;
        }
    }
    
    checkForThreats(unit) {
        const closestEnemy = this.game.combatSystem.findTarget(unit, unit.sightRange);
        if (closestEnemy) {
            unit.aiTarget = closestEnemy;
            unit.aiState = 'chase';
        }
    }
    
    updateChase(unit) {
        if (!unit.aiTarget || !unit.aiTarget.isAlive) {
            unit.aiState = 'idle';
            unit.aiTarget = null;
            return;
        }
        
        const distance = unit.position.distanceTo(unit.aiTarget.position);
        
        if (distance <= unit.attackRange) {
            unit.aiState = 'attack';
            unit.isAttacking = true;
            unit.targetEntity = unit.aiTarget;
        } else {
            unit.moveTo(unit.aiTarget.position);
        }
    }
    
    updateAttack(unit) {
        if (!unit.aiTarget || !unit.aiTarget.isAlive) {
            unit.aiState = 'idle';
            unit.isAttacking = false;
            unit.targetEntity = null;
            return;
        }
        
        const distance = unit.position.distanceTo(unit.aiTarget.position);
        
        if (distance > unit.attackRange * 1.2) {
            unit.aiState = 'chase';
            unit.isAttacking = false;
        }
        
        if (unit.health < unit.maxHealth * 0.2) {
            unit.aiState = 'flee';
            unit.isAttacking = false;
            unit.targetEntity = null;
        }
    }
    
    updatePatrol(unit) {
        if (!unit.patrolPoints || unit.patrolPoints.length === 0) return;
        
        const target = unit.patrolPoints[unit.patrolPointIndex || 0];
        const distance = unit.position.distanceTo(target);
        
        if (distance < 1) {
            unit.patrolPointIndex = ((unit.patrolPointIndex || 0) + 1) % unit.patrolPoints.length;
        } else {
            unit.moveTo(target);
        }
    }
    
    updateFlee(unit) {
        if (unit.health >= unit.maxHealth * 0.5) {
            unit.aiState = 'idle';
            return;
        }
        
        if (!unit.aiTarget || !unit.aiTarget.isAlive) {
            unit.aiState = 'idle';
            return;
        }
        
        const fleeDirection = unit.position.clone().sub(unit.aiTarget.position).normalize();
        const fleeTarget = unit.position.clone().add(fleeDirection.multiplyScalar(10));
        unit.moveTo(fleeTarget);
    }
    
    cleanup() {
        this.aiUnits = this.aiUnits.filter(u => u.isAlive);
    }
}

export default AISystem;
```

**Step 2: 在 Game.js 中集成 AISystem**

```javascript
import AISystem from '../systems/AISystem.js';

// 在构造函数中:
this.aiSystem = null;

// 在 initIndependentSystems() 中:
this.aiSystem = new AISystem(this);

// 在 update() 中:
this.aiSystem.update(deltaTime);
```

**Step 3: 在 Game.initArabiaEntities() 和 initTestUnits() 中注册敌方单位到 AI**

对于 owner === 'enemy' 的单位：

```javascript
if (this.aiSystem && unit.owner !== 'player') {
    unit.aiState = 'idle';
    unit.aiTarget = null;
    this.aiSystem.registerUnit(unit);
}
```

**Step 4: 在 Unit.js 中添加 AI 相关属性**

在构造函数中添加：

```javascript
this.aiState = 'idle';
this.aiTarget = null;
this.patrolPoints = [];
this.patrolPointIndex = 0;
```

**Step 5: 运行 `npm run dev`，移动玩家单位到敌方单位附近，观察敌方单位自动追击和攻击**

---

## Task 7: 实现生产进度 UI

**Files:**
- Modify: `src/ui/ActionPanel.js`
- Modify: `src/ui/InfoPanel.js`

**Step 1: 在 ActionPanel.js 中添加生产队列显示**

当选中建筑且建筑有生产队列时，在按钮区域显示当前生产进度。

```javascript
updateProductionUI(building) {
    if (!building || building.type !== 'building') return;
    
    const buttons = this.buildingPanelConfig.buttons;
    if (!buttons || buttons.length === 0) return;
    
    if (building.currentProduction) {
        const firstBtn = this.container?.querySelector('.building-btn');
        if (firstBtn) {
            const progress = building.productionProgress || 0;
            firstBtn.style.background = 
                `linear-gradient(to right, rgba(0,255,0,0.3) ${progress}%, transparent ${progress}%)`;
        }
    }
}
```

**Step 2: 在 HUD 的 update 循环中调用生产进度更新**

当有建筑被选中时，每帧更新进度条显示。

**Step 3: 运行 `npm run dev`，选中正在生产的建筑，观察进度条变化**

---

## Task 8: 实现基础科技研究

**Files:**
- Modify: `src/core/Game.js`
- Modify: `src/entities/Player.js`

**Step 1: 在 Player.js 中添加已研究科技列表**

```javascript
// 在构造函数中:
this.researchedTechs = new Set();

// 添加方法:
hasResearched(techType) {
    return this.researchedTechs.has(techType);
}

completeResearch(techType) {
    this.researchedTechs.add(techType);
    this.applyTechEffects(techType);
}

applyTechEffects(techType) {
    const effects = {
        loom: () => {
            this.units.forEach(u => { u.health += 15; u.maxHealth += 15; u.armor += 1; });
        },
        town_watch: () => {
            // 增加城镇中心视野范围
        }
    };
    if (effects[techType]) effects[techType]();
}
```

**Step 2: 在 Game.js 中实现 applyResearch**

```javascript
applyResearch(building, techType) {
    if (this.player) {
        this.player.completeResearch(techType);
    }
    console.log(`[Game] 科技研究完成: ${techType}`);
}
```

**Step 3: 运行 `npm run dev`，选中城镇中心，研究"织布机"，观察村民属性变化**

---

## Task 9: 集成验证与回归测试

**Step 1: 运行 `npm run build` 确认无构建错误**

**Step 2: 手动测试清单**

- [ ] 游戏启动，选择地图进入
- [ ] 放置建筑（房屋），观察建造动画
- [ ] 建造完成后，人口上限增加
- [ ] 选中城镇中心，生产村民
- [ ] 村民右键采集资源正常
- [ ] 右键敌方单位，己方单位攻击
- [ ] 敌方单位生命值降至 0，死亡动画播放
- [ ] 敌方单位靠近己方单位时自动追击
- [ ] 敌方单位生命值低时逃跑
- [ ] 选中城镇中心，研究织布机
- [ ] 研究完成后村民护甲增加

**Step 3: 运行 `npm run build` 确认构建成功**

---

## 执行顺序与依赖关系

```
Task 0 (右键取消 + 双触发修复) ✅
Task 1 (修复放置 Bug)           ✅ (Bug 已不存在)
Task 2 (放置接入 Game)          ✅
  └── Task 3 (建造完成回调)      ⏳
       ├── Task 4 (生产单位)     ⏳
       └── Task 5 (单位死亡)     ⏳ 并行
            └── Task 6 (AI 系统)  ⏳
Task 4 ──→ Task 7 (生产 UI)     ⏳ 并行 Task 8
Task 4 ──→ Task 8 (科技研究)    ⏳ 并行 Task 7
                              └──→ Task 9 (集成验证)
```

建议按 Task 3→4→5→6→7→8→9 顺序执行，其中 Task 4 和 Task 5 可以并行。
