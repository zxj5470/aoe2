# 帝国时代2 Web版开发清单

更新时间：2026-05-12

## 当前判断

- 当前项目处于“可玩的 alpha 原型”阶段。
- 核心闭环已打通：地图生成、实体生成、移动寻路、资源采集、建筑放置、基础建造、部分生产、HUD。
- 当前主要问题不在“有没有功能”，而在“系统是否收口”和“中层玩法是否补齐”。
- 以代码实际状态判断，更接近 `roadmap.md` 中的 `~68%`，不建议按 `currentProcess.md` 的 `87%` 评估。

## 开发目标

- 先修掉当前影响功能稳定性的结构问题。
- 再补齐 RTS 核心玩法缺失项。
- 最后处理体验层和性能层完善。

---

## P0 立即处理

### 1. 统一建筑 ID 命名

问题：
- 当前 `ActionPanel.js`、`BuildingPlacementSystem.js`、`BuildingBase.js` 同时存在连字符、下划线、简写三套命名。
- 已知高风险项：
  - `archery-range`
  - `archery`
  - `archery_range`
  - `watch-tower`
  - `watch_tower`

影响：
- 建筑按钮点击后可能无法进入放置。
- 建筑生成后可能拿不到正确的尺寸、外观、功能配置。
- 后续生产面板、建筑功能、AI 引用都会继续踩坑。

涉及文件：
- `src/ui/ActionPanel.js`
- `src/systems/BuildingPlacementSystem.js`
- `src/entities/BuildingBase.js`
- `src/core/Game.js`
- `src/core/EntityManager.js`

建议做法：
- 统一内部标准为下划线命名。
- UI 层如果需要连字符，只在展示层做映射。
- 提取一个建筑类型规范化函数，避免各处手写 `replace(/-/g, '_')`。

验收标准：
- 所有建筑按钮都能正常进入放置。
- 放置后的建筑尺寸、外观、功能与配置一致。
- `archery_range`、`watch_tower`、`town_center` 等关键建筑不再有分支命名。

### 2. 收敛战斗执行链

问题：
- 当前存在两套战斗执行逻辑：
  - `src/entities/UnitCombat.js`
  - `src/systems/CombatSystem.js`
- 两边都在处理攻击、追击、攻击冷却。

影响：
- 逻辑重复，后续改平衡会改两次。
- 容易出现伤害重复结算、行为不一致、AI 与手操逻辑分裂。
- 文档上看像“战斗系统已完成”，代码上实际是“半收口状态”。

涉及文件：
- `src/entities/Unit.js`
- `src/entities/UnitCombat.js`
- `src/systems/CombatSystem.js`
- `src/core/EntityManager.js`
- `src/input/SelectionManager.js`

建议做法：
- 明确只保留一套实际伤害结算路径。
- 推荐保留 `CombatSystem` 作为全局战斗仲裁。
- `UnitCombat` 收缩为命令入口和单位状态辅助，不再直接结算伤害。
- 补齐初始单位注册逻辑，不要只注册生产出来的新单位。

验收标准：
- 所有可战斗单位都统一注册到 `CombatSystem`。
- 玩家手动攻击和 AI 攻击走同一条伤害结算链。
- 伤害、护甲、克制、攻击冷却只定义一份。

### 3. 修正文档与版本号漂移

问题：
- `README.md` / `roadmap.md` / `currentProcess.md` / `package.json` 的版本状态不一致。

影响：
- 后续开发判断容易失真。
- 你自己回看项目时也会被旧状态误导。

涉及文件：
- `README.md`
- `roadmap.md`
- `currentProcess.md`
- `package.json`

建议做法：
- 统一当前版本号。
- 统一当前阶段描述。
- 明确“已完成 / 已接通 / UI 已有但逻辑未完成”的边界。

验收标准：
- 所有项目总览文档对版本和阶段描述一致。

---

## P1 短期完成

### 4. 补齐兵营/靶场/马厩生产面板

现状：
- `BuildingBase.js` 已有建筑功能定义。
- `BuildingProduction.js` 已有生产队列和完成逻辑。
- `ActionPanel.js` 当前真正接通的生产面板主要还是 `town_center_production`。

影响：
- 生产系统从代码结构上看已经存在，但玩家视角仍然不完整。
- 这会让“兵营类建筑已完成”停留在数据层，不是玩法层。

涉及文件：
- `src/ui/ActionPanel.js`
- `src/entities/BuildingBase.js`
- `src/entities/BuildingProduction.js`
- `src/core/EntityManager.js`
- `src/core/Game.js`

建议做法：
- 为以下建筑补生产预设：
  - `barracks_production`
  - `archery_range_production`
  - `stable_production`
- 按建筑能力限制可生产单位，不要在 UI 里硬编码错配。
- 增加生产中、队列中、完成后的 UI 提示。

验收标准：
- 选中兵营、靶场、马厩时出现对应生产面板。
- 扣资源、排队、计时、生成单位流程可完整跑通。

### 5. 补齐建筑建造取消与建造中 UI

现状：
- 建造进度本体已经存在。
- 但路线图里提到的“取消建造”“清晰建造中反馈”还不完整。

涉及文件：
- `src/entities/BuildingConstruction.js`
- `src/entities/BuildingBase.js`
- `src/ui/InfoPanel.js`
- `src/ui/ActionPanel.js`
- `src/input/SelectionManager.js`

建议做法：
- 为未完成建筑增加取消入口。
- 取消时明确退还资源策略。
- 在信息面板里显示：
  - 建造状态
  - 建造进度
  - 建造者数量

验收标准：
- 未完工建筑可取消。
- UI 可看到明确进度。
- 取消逻辑不会留下占格、碰撞、建造者状态残留。

### 6. 补齐防御建筑自动攻击

现状：
- `BuildingBase.js` 已声明 `watch_tower` 有攻击能力。
- 但自动攻击链路还没真正接通。

涉及文件：
- `src/entities/BuildingBase.js`
- `src/entities/Building.js`
- `src/systems/CombatSystem.js`
- `src/systems/AISystem.js`

建议做法：
- 让防御建筑也进入统一战斗系统。
- 明确攻击范围、攻击频率、目标选择优先级。

验收标准：
- 瞭望塔会自动攻击进入范围的敌方单位。
- 不误伤友军。
- 攻击表现和伤害逻辑与单位系统一致。

### 7. 补齐属性升级与科技效果框架

现状：
- `Player.js` 已支持 `researchedTechs` 和少量效果。
- 现在仍属于单点实现，不是完整科技框架。

涉及文件：
- `src/entities/Player.js`
- `src/ui/ActionPanel.js`
- `src/entities/BuildingBase.js`
- `src/core/Game.js`

建议做法：
- 先做一个最小科技框架：
  - 科技定义表
  - 前置校验
  - 研究时间
  - 一次性效果应用
- 优先完成：
  - `loom`
  - `town_watch`
  - 基础兵种攻击/护甲升级

验收标准：
- 科技能被研究、阻止重复研究、正确应用效果。
- UI 能反映已研究状态。

---

## P2 中期完善

### 8. 强化 AI 为真正的敌军行为系统

现状：
- `AISystem.js` 已有 `idle/chase/attack/flee`。
- 但还没有巡逻、警戒、编队、建筑目标、经济目标等层次。

涉及文件：
- `src/systems/AISystem.js`
- `src/systems/CombatSystem.js`
- `src/core/EntityManager.js`

建议做法：
- 先补巡逻和守卫。
- 再补目标优先级：
  - 最近敌人
  - 攻击自己的单位
  - 高价值单位
- 最后再考虑敌军出兵或波次逻辑。

验收标准：
- 敌军单位不再只是被动挨打。
- AI 行为稳定，不出现频繁抖动或目标重置。

### 9. 完善时代系统

现状：
- 玩家时代等级、罗马数字显示、升级按钮基础已存在。
- 但时代解锁内容和建筑/单位限制还没真正连起来。

涉及文件：
- `src/entities/Player.js`
- `src/ui/ActionPanel.js`
- `src/entities/BuildingBase.js`
- `src/config.js`

建议做法：
- 建立时代解锁表。
- 根据时代决定：
  - 可建建筑
  - 可产单位
  - 可研究科技

验收标准：
- 升级时代后确实解锁新内容。
- 未到时代时按钮不可用或明确提示。

### 10. 补战争迷雾

这是当前最缺“RTS味”的功能之一。

涉及文件：
- `src/world/Map.js`
- `src/ui/Minimap.js`
- `src/entities/Unit.js`
- `src/entities/Building.js`
- `src/core/Scene.js`

建议做法：
- 先做逻辑层可见格。
- 再做渲染层遮罩。
- 最后同步到小地图。

验收标准：
- 未探索区域不可见。
- 已探索但当前无视野区域半可见。
- 小地图同步表现。

---

## P3 体验与性能

### 11. 性能优化第一轮

目标：
- 在不大改架构的前提下，先处理最明显瓶颈。

建议项：
- 视锥裁剪
- 减少 UI 高频刷新
- 限制调试日志输出
- 检查实体 update 的无效遍历
- 梳理碰撞与空间索引更新频率

涉及文件：
- `src/core/Game.js`
- `src/core/UIManager.js`
- `src/core/SpatialIndex.js`
- `src/systems/CollisionSystem.js`
- `src/entities/`

验收标准：
- 大量单位/资源场景下帧率明显更稳。

### 12. 存档、音效、最终体验层

这部分现在都不该放在最前面，等 P0-P2 稳定后再做。

建议顺序：
- 音效反馈
- 存档结构设计
- 自动存档
- 简单设置面板

---

## 推荐执行顺序

### 第一轮

1. 统一建筑 ID 命名
2. 收敛战斗执行链
3. 修正文档与版本号

### 第二轮

1. 补齐兵营/靶场/马厩生产面板
2. 补齐建造取消和建造中 UI
3. 接通防御建筑自动攻击

### 第三轮

1. 补科技与属性升级框架
2. 强化 AI
3. 完善时代系统

### 第四轮

1. 战争迷雾
2. 性能优化
3. 音效与存档

---

## 建议作为最近里程碑的交付标准

如果你想先做一个“v0.5.1 真正成立”的版本，我建议最少完成这些项：

- 建筑命名统一
- 战斗系统收口
- 兵营/靶场/马厩生产面板可用
- 建造进度/取消可用
- 防御建筑自动攻击可用
- 文档与版本信息同步

完成这批之后，这个项目才比较像一个结构稳定的 `v0.5.1-alpha`，否则更像“功能多，但系统未收口的 v0.5.0+”。
