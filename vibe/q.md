## v1
1. 资源面板要在上方。
2. 下方面板内容分三块：左侧是建筑模式（普通、兵营、以及）面板。中间是建筑单位信息。右侧是小地图。
3. 鼠标移动至边缘应该是平移地图，不应该是旋转地图。
4. 整体小地图内部主区域要是一个菱形的形状。

## v2
1. 资源面板分别是：肉、木、金矿、石料；房子单位 1/20。从左上角开始而不是上方居中。上方居中需要显示当前所在时代【黑暗时代】
2. 左下建筑模式区域需要 3*4 的格子。
3. 整体相机应该是45度俯视的效果。目前鼠标移动至边缘的平移仍然是改变旋转角，而不是期望的直接改变相机位置。相机的朝向方向应该是永远固定的，只有坐标改变。

## v3
1. 不需要【建筑模式】这几个字。保证左下角有3行4列，如果没有内容可以是空白占位符，另外emoji与文字允许重叠（文字在方块的右下，emoji图标在正中间）。
2. 点击小地图要能跳转大场景位置。
3. 左上的每一种资源的数量要增大横向间隔
4. 目前鼠标放置在边缘触发的仍然是旋转。请检查实现逻辑。请询问我更多细节。
5. 场景以外的位置使用黑色。

## v4
1. 将鼠标移到边缘时，看到的旋转是改变俯仰角在变化。我希望的是强制固定相机视角是45度俯视向下。如果你还不理解，请询问具体细节。
2. 删除这个项目中的雾效。

（介入一次，相机代码的target存在问题）

## v5
1. 当前状态：相机完全是垂直90度的俯视状态；而我希望的是45度俯视。

## v6
1. 当前状态：鼠标移动到最左侧会让视角往上。鼠标移动到最右侧会让视角向下。在界面最上方和最下方没有正常触发。 
没有解决问题
【现在的问题是小地图的上下左右与按键对应；但是大场景是不对应的】
有没有一种可能，需要将大地图的场景相机旋转90度

## v7
初始化的视角与按过一次任意按键的视角不对应。初始化的视角仍然是垂直向下。

## v8
点击小地图后视角又会变成垂直向下。另外小地图的坐标与真实情况不对应。目前情况点击小地图的左上部分才是地图的中心。

## v9
1. 修改左下角宽度大小，每个格子需要是正方形的4行 3列，现在的情况是3行4列。（并且这个面板需要做成变量形式能够切换）
